/**
 * DEPRECATED (2026-08-18) — this was the rejected first pass at the Cash Flow
 * projection's timing/inflow helpers. Nothing imports it. The real implementation is
 * lib/cashflow/cashProjection.js (per-account accrual + cash-timing math and the
 * Customer-tab inflow handoff) and lib/cashflow/useCashTimingState.js (persistence).
 * Kept only because this file can't be deleted from the current session's mount —
 * safe to remove outright.
 */
export {};
